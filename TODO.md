# TODO — VS MOTORS

Задачи по этапам разработки. Текущий этап отмечен **▶**.

---

## Этап 0 — Инфраструктура и скелет проекта
**Цель:** поднять все сервисы, сделать hello world.

- [ ] Создать Telegram бота через @BotFather, получить `BOT_TOKEN`
- [ ] Зарегистрировать аккаунт Supabase, создать проект (регион: eu-central-1 Frankfurt)
- [ ] Зарегистрировать аккаунт Cloudinary, получить `CLOUDINARY_URL`
- [ ] Зарегистрировать аккаунт Render (бэкенд) + Vercel (фронтенд)
- [ ] Зарегистрировать аккаунт UptimeRobot, добавить мониторинг `/health`
- [ ] Создать репозиторий GitHub (структура: `/backend`, `/frontend`)
- [ ] Инициализировать бэкенд: FastAPI + aiogram 3 + asyncpg, Dockerfile
- [ ] Инициализировать фронтенд: Vite + React 18 + TypeScript
- [ ] Задеплоить скелет на Render + Vercel
- [ ] Убедиться что `/health` возвращает `{"status": "ok", "db": "ok"}`
- [ ] Настроить все ENV переменные (по списку из SPEC.md)

---

## Этап 1 — База данных
**Цель:** создать таблицу `listings` и миграции.

- [ ] Написать SQL-миграцию: таблица `listings` (по схеме из SPEC.md)
- [ ] Добавить индексы (status, created_at DESC)
- [ ] Применить миграцию в Supabase SQL Editor
- [ ] Добавить 3–5 тестовых объявлений вручную через Supabase UI
- [ ] Проверить что asyncpg подключается (`prepared_statement_cache_size=0`)

---

## Этап 2 — API: публичные эндпоинты
**Цель:** GET /api/listings и GET /api/listings/{id}.

- [ ] `GET /api/listings` — все активные, пагинация (page, page_size), сортировка created_at DESC
- [ ] `GET /api/listings/{id}` — детали одного объявления (включая `sold`)
- [ ] Протестировать через curl / httpx
- [ ] CORS: настроить через `CORS_ORIGINS` ENV

---

## Этап 3 — Авторизация владельца
**Цель:** верификация initData + защита owner-эндпоинтов.

- [ ] Функция верификации Telegram `initData` (HMAC-SHA256 с `BOT_TOKEN`)
- [ ] Middleware или dependency: извлечь `telegram_id`, сравнить с `OWNER_TELEGRAM_ID`
- [ ] Вернуть `403` если не владелец
- [ ] Протестировать: валидный initData владельца → OK; чужой → 403; невалидный → 401

---

## Этап 4 — API: owner-эндпоинты
**Цель:** CRUD объявлений для владельца.

- [ ] `POST /api/listings` — создать объявление
- [ ] `PATCH /api/listings/{id}` — обновить поля (частичное обновление)
- [ ] `DELETE /api/listings/{id}` — удалить физически
- [ ] `GET /api/admin/listings` — все объявления включая `sold`
- [ ] `POST /api/listings/{id}/photos` — получить signed upload parameters Cloudinary
- [ ] `DELETE /api/listings/{id}/photos` — удалить фото из массива (URL в body)
- [ ] Проверить что endpoint'ы недоступны без корректного `X-Telegram-Init-Data`

---

## Этап 5 — Telegram бот
**Цель:** бот отвечает на команды и открывает Mini App.

- [ ] Хендлер `/start` — приветствие + inline кнопка «Открыть VS MOTORS» (web_app)
- [ ] Хендлер `/help` — краткая справка
- [ ] Обработка `startapp` параметра (deep link) — передать в Mini App через `start_param`
- [ ] Зарегистрировать команды через `set_my_commands`
- [ ] Настроить Menu Button в BotFather (web_app, URL = MINI_APP_URL)
- [ ] Протестировать: `/start` открывает Mini App

---

## Этап 6 — Фронтенд: скелет и роутинг
**Цель:** React-приложение с роутингом и темой Telegram.

- [ ] Установить `@twa-dev/sdk`, `react-router-dom`
- [ ] Настроить `Telegram.ready()` при старте
- [ ] Настроить CSS переменные из `Telegram.themeParams` (тёмная/светлая тема)
- [ ] Реализовать все маршруты из SPEC.md (пустые компоненты-заглушки)
- [ ] Компонент-guard для страниц владельца (редирект если не owner)
- [ ] Определение `isOwner`: `Telegram.initDataUnsafe.user.id === Number(import.meta.env.VITE_OWNER_ID)`

> ⚠️ `VITE_OWNER_ID` нужен на фронтенде только для показа/скрытия UI-элементов. Реальная проверка прав — только на бэкенде.

---

## Этап 7 — HomePage
**Цель:** витрина автомобилей с бесконечной прокруткой.

- [ ] Компонент `CarCard` — по макету из SPEC.md
- [ ] `HomePage` — запрос `GET /api/listings`
- [ ] Бесконечная прокрутка: IntersectionObserver, загружать следующую страницу при приближении к концу
- [ ] Состояние загрузки (skeleton cards)
- [ ] Пустое состояние («Объявлений пока нет»)
- [ ] Кнопка «Добавить» — только если `isOwner`

---

## Этап 8 — ListingDetailPage
**Цель:** полная страница автомобиля с контактами.

- [ ] Запрос `GET /api/listings/{id}`
- [ ] Фотогалерея: горизонтальный свайп + точки пагинации
- [ ] Таблица характеристик (все поля из SPEC.md)
- [ ] Описание (с обрезкой «показать больше» если >300 символов)
- [ ] Статус-бейдж: «Продаётся» (зелёный) / «Продан» (серый)
- [ ] Блок контактов:
  - [ ] «Написать в Telegram» → `tg://resolve?domain={VITE_OWNER_TG_USERNAME}`
  - [ ] «Написать в WhatsApp» → `https://wa.me/{VITE_OWNER_WHATSAPP}` (если задан)
  - [ ] «Позвонить» → `tel:{VITE_OWNER_PHONE}` (если задан)
- [ ] «Поделиться» → clipboard + toast (синхронно, без await перед writeText)
- [ ] Кнопки «Редактировать» / «Удалить» — только если `isOwner`
- [ ] `Telegram.BackButton` — назад в витрину

---

## Этап 9 — AddListingPage / EditListingPage
**Цель:** форма добавления и редактирования (только владелец).

- [ ] Многошаговая форма (4 шага по SPEC.md)
- [ ] Шаг 1: Основное (марка, модель, год, цена, статус)
- [ ] Шаг 2: Характеристики (все поля по SPEC.md)
- [ ] Шаг 3: Фото (загрузка через Cloudinary signed upload) + описание
- [ ] Шаг 4: Предпросмотр + «Опубликовать»
- [ ] Компонент загрузки фото:
  - [ ] Клик для выбора файлов
  - [ ] Перетаскивание для изменения порядка
  - [ ] Миниатюры с кнопкой удаления
  - [ ] Прогресс-бар загрузки каждого фото
  - [ ] Лимит 20 фото
- [ ] При редактировании — предзаполнить форму текущими данными
- [ ] После успешного сохранения — редирект на ListingDetailPage
- [ ] Защита маршрута — редирект на `/` если не владелец

---

## Этап 10 — AdminPage
**Цель:** управление всеми объявлениями.

- [ ] Список всех объявлений (включая `sold`)
- [ ] Быстрое переключение статуса `active` ↔ `sold`
- [ ] Кнопка «Изменить» → EditListingPage
- [ ] Кнопка «Удалить» → подтверждение (`Telegram.showConfirm`) → DELETE запрос
- [ ] Кнопка «+ Добавить» → AddListingPage
- [ ] Защита маршрута — редирект на `/` если не владелец

---

## Этап 11 — Deep links
**Цель:** ссылки на конкретный автомобиль открывают нужную страницу.

- [ ] При старте Mini App проверять `Telegram.initDataUnsafe.start_param`
- [ ] Если `start_param` начинается с `listing_` → редирект на `/listing/{id}`
- [ ] Кнопка «Поделиться» на ListingDetailPage строит deep link: `https://t.me/{BOT_USERNAME}?startapp=listing_{id}`
- [ ] Протестировать: внешняя ссылка → открывает Mini App на нужном авто

---

## Этап 12 — Полировка и продакшн
**Цель:** готово к реальному использованию.

- [ ] Проверить все ENV в продакшне (Render + Vercel)
- [ ] Пройти TESTING_CHECKLIST.md полностью
- [ ] UptimeRobot: убедиться что `/health` пингуется каждые 5 минут
- [ ] Протестировать на реальном телефоне (iOS + Android)
- [ ] Проверить тёмную и светлую темы Telegram
- [ ] Проверить все кнопки контактов (TG, WA, телефон)
- [ ] Добавить `og:image` и `og:title` на фронтенд (для красивых превью ссылок)
- [ ] Настроить автодеплой: main branch → Render + Vercel

---

*Последнее обновление: май 2026*
